import { useContext } from "react"
import BodyContext from "../contextFile"

export default function AboutPage() {
    return (<>
    
        <p className="mt-[10rem]">This is the About page</p>

    </>)
}